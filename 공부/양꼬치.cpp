#include <string>
#include <vector>
#include <iostream>

using namespace std;

int solution(int n, int k) {
    int answer = 0;
    return answer = (12000 * n) + ((k - (n / 10)) * 2000);
}

int main()
{
    cout << solution(10,3);
}